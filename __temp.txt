The main work remaining falls under Phase 5: PWA & Polish, along with a few advanced AI features. Here is the list of remaining tasks:

📱 Phase 5: PWA, Infrastructure & Settings (Remained)
1. Infrastructure & Security
 Configure PWA: Finalize vite-plugin-pwa settings (manifest, icons, and service worker strategy) to make the app installable and available offline.
 Nginx Reverse Proxy: Add an Nginx service to docker-compose.yml to combine the frontend and backend on a single port (cleaning up the dual-port 5173/8000 setup).
 Security Hardening: Move the Google service account JSON keys outside the web-accessible root and use environment variables for paths.
2. Settings & Customization
 Build /settings Page:
Profile management (name, email, password).
Categories Manager: Add, rename, or delete custom categories.
Currency/Locales: Global settings for currency symbols and date formats.
 Transaction CSV Export: While reports have export, adding a "bulk export" to the main Transactions ledger.
3. Advanced AI & UI Polish
 Auto-Categorization: Implement a backend service to suggest a category based on the merchant name (e.g., "Menards" automatically suggests "Repairs").
 Fuzzy Matching: Improve the matching engine using Levenshtein distance for merchant names to catch matches even if the names aren't 100% identical.
 Responsive Refinement: Detailed CSS polish for smaller mobile screens to ensure the dashboard and reports are perfectly usable on phones.
Recommendation
I recommend starting with Phase 5: PWA & Settings.

PWA Support is a core goal that makes the app feel like a premium tool on mobile.
Settings (Categories Manager) is practically necessary now that users can import data—they need a way to customize their bookkeeping.